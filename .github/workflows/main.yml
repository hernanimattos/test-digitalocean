name:  Build & Deploy
on:
  push: 
    branches: [main]
    
jobs:
  deploy: 
    runs-on: ubuntu-latest
    steps: 
      - name: Deploy teste
        uses: appleboy/ssh-action@v0.1.2
        with:
          host: ${{secrets.SHH_HOST}}
          key: ${{secrets.SSH_KEY}}
          username: ${{secrets.SSH_USERNAME}}
script: |
  mkdir test
  cd test
  git clone https://github.com/hernanimattos/test-digitalocean.git
  echo "Deu Certo"
    
